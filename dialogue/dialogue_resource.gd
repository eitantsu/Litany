extends Resource

# dialogue resource, holds textures and text for each NPC

class_name DialogueResource

export(Texture) var player_texture
export(Texture) var npc_texture
export(Array,String) var lines
